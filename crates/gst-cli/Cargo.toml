[package]
name = "gst-cli"
description = "Prepare Indian GST returns offline: validate workbooks, generate portal upload JSON"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "gst"
path = "src/main.rs"

[dependencies]
clap.workspace = true
rust_decimal.workspace = true
gst-core = { path = "../gst-core" }
